import styled from 'styled-components'

export const TableGroupContainer = styled.tr`
    td{
        &.title{
            display: flex;
            align-items: center;
            gap: 10px;
        }
    }
`
